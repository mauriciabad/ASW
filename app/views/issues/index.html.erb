<p id="notice"><%= notice %></p>

<h1>Issues</h1>

<br/>

<div class="clearfix" id="issue-toolbar">
  <span class="filter-label">Filter by:</span>
  <a class="filter" href="">All</a>
  <a class="filter" href="">Open</a>
  <a class="filter" href="">My issues</a>
  <a class="filter" href="">Watching</a>
</div>

<br/>

<table style="width:100%;text-align:center">
  <thead>
    <tr>
      <th >Title</th>
      <!--<th>Description</th>-->
      <th>Type</th>
      <th>Priority</th>
      <th>Status</th>
      <th>Votes</th>
      <th>User</th>
      <th>Created</th>
      <th>Updated</th>
      <th colspan="2"></th>
    </tr>
  </thead>
  
  <tbody>
    <% @issues.each do |issue| %>
      <tr>
        <td><%= link_to issue.title, issue %></td>
        <!--<td><%= issue.description %></td>-->
        <td><%= issue.kind %></td>
        <td><%= issue.priority %></td>
        <td>status</td>
        <td>votes</td>
        <td><%= issue.user_id %></td>
        <td><%= issue.created_at %></td>
        <td>caca</td>
        <td><%= link_to image_tag('eye_1.svg', size: '16'), issue %></td>
        <td><%= link_to image_tag('pen_1.svg', size: '16'), edit_issue_path(issue) %></td>
        <td><%= link_to image_tag('delete_1.svg', size: '16'), issue, method: :delete, data: {confirm: 'Are you sure?'} %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Issue', new_issue_path %>

